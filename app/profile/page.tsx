import Profile from "@/containers/profile/Profile";

export const metadata = {
  title: "Profile",
};

const page = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default page;
