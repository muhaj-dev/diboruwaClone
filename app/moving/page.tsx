import MovingContainer from "@/containers/moving/Moving";

export const metadata = {
  title: "Moving",
};

const page = () => {
  return (
    <div>
      <MovingContainer />
    </div>
  );
};

export default page;
