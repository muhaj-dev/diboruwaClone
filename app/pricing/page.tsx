import Pricing from "@/containers/pricing/Pricing";

export const metadata = {
  title: "Pricing",
};

const page = () => {
  return (
    <div>
      <Pricing />
    </div>
  );
};

export default page;
